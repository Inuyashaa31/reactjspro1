import React from 'react';

function Skills() {
    const skills = [
        "DSA",
        "Python",
        "C",
        "ReactJs",
        "mongoDB",
        "NodeJS",
    ];
    return (
        <div className='py-5'>
            <h1 className='text-notso text-2xl'>
                Skills set:
            </h1>
            {skills.map((skills, index) => (
                <div>
                    <div />
            ))}
                </div>
            ))}

            export default Skills
        </>);
}
